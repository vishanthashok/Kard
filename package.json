{
  "name": "kard",
  "version": "0.1.0",
  "private": true,
  "description": "Kard — universal rewards card for west campus",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push --skip-generate",
    "db:setup": "prisma generate && prisma db push --skip-generate",
    "seed": "node prisma/seed.js",
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
